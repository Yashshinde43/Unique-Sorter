import QuotationForm from '@/components/QuotationForm';

export const metadata = { title: 'New Quotation — UniqueSorter CRM' };

export default function NewQuotationPage() {
  return <QuotationForm />;
}
